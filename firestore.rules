rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // PuntoReview currently accesses Firestore through the trusted server.
    // Direct browser reads/writes are denied by default.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
